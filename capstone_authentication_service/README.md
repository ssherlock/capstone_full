# Authentication REST Server
 
Operations to save a user, log a user in (returning a JWT token) and validate a token is still valid.

## Services 
### Can be seen using Swagger - http://localhost:8084/swagger-ui.html#/
 
